<?xml version="1.0" encoding="UTF-8"?>
<Grid xmlns="http://xamarin.com/schemas/2014/forms" 
      xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
      x:Class="InterTwitter.Controls.NotificationPageNavigationBar"
      xmlns:resources="clr-namespace:InterTwitter.Resources" 
      VerticalOptions="Start"
      HorizontalOptions="FillAndExpand"
      ColumnSpacing="0"
      RowSpacing="0"
      HeightRequest="60"
      BackgroundColor="{StaticResource bg_1}">

    <Grid.ColumnDefinitions>
        <ColumnDefinition Width="40"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="40"/>
    </Grid.ColumnDefinitions>

    <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="1"/>
    </Grid.RowDefinitions>

    <Label Grid.Column="1"
           Grid.Row="0"
           Text="{x:Static resources:AppResource.NotificationPageTitle}"
           FontSize="18"
           TextColor="{StaticResource tx_3}"
           FontAttributes="Bold"
           VerticalOptions="Center"
           HorizontalOptions="Center"/>

    <BoxView Grid.Column="0" 
             Grid.ColumnSpan="3"
             Grid.Row="1"
             HeightRequest="1"
             BackgroundColor="{StaticResource bg_3}"
             HorizontalOptions="FillAndExpand"
             VerticalOptions="EndAndExpand"/>

</Grid>